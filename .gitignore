node_modules/
.bin/
assets/frames/
out/
*.tmp

# web app (Next.js)
.next/
next-env.d.ts
*.tsbuildinfo

# secrets / local env (never commit API keys)
.env
.env.*
!.env.example

# vector memory store (generated at runtime when @xenova/transformers is enabled)
brain/vector-memory.json
