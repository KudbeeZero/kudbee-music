{
  "note": "The living-state spine — every roadmap item + the PR/status that shipped it (see livingState below for the full sync rule). Read by lib/hermes/statusBoard.ts to generate STATUS.md + the STATUS blocks in CLAUDE.md/README.md/BUILD_LOG.md. See brain/README.md for the full brain/ folder index.",
  "updated": "2026-07-03",
  "northStar": "The AI-engine pillar of WIFI DJ (wifidj.xyz). The lyrical process + the brain are the moat — not the video studio.",
  "principles": [
    "$0 / local / original-only by default",
    "no living-artist mimicry — influences are felt, never copied",
    "assistant, not autopilot — the artist decides",
    "real-AI and Web3 are opt-in, behind adapters/APIs, never bloating the free local core"
  ],
  "livingState": {
    "note": "One source of truth so nothing goes stale or gets left behind.",
    "capture": "IDEAS.md — the capture net; nothing the artist says is lost",
    "backlog": "TODO.md — the working list",
    "spine": "brain/roadmap.json — this file; indexes every item + the PR that shipped it",
    "highlights": "README.md#-roadmap — the highlight reel",
    "rule": "Every PR updates the spine + TODO + IDEAS + README together. Shipping an item flips its status here and moves it to Shipped in TODO. A PR that skips this is not done."
  },
  "brainRegions": 11,
  "brainSubregions": 37,
  "tractionSprint": {
    "note": "Ranked $0 items from the 3-agent GitHub-traction audit. Theme: make display-only brain systems load-bearing, wire in vector memory, document the architecture.",
    "items": [
      { "id": "T0", "title": "Vector-memory determinism hardening", "status": "shipped", "pr": "#49", "home": "lib/hermes/vectorMemory.ts" },
      { "id": "T1", "title": "Semantic originality — meaning-level novelty (optional, server-side)", "status": "shipped", "pr": "#54", "home": "lib/hermes/semanticOriginality.ts" },
      { "id": "T2", "title": "Close the cognition loop — cognition picks the hook + regenerate-from-critiques", "status": "shipped", "pr": "#52", "home": "cognition.ts (selectHookByCognition) + pipeline.ts (cognitionFeedback) + SongPackageView.tsx + HermesHitFactory.tsx" },
      { "id": "T3", "title": "Interactive trace explorer + demo gallery", "status": "shipped", "pr": "#50", "home": "lib/hermes/traceHtml.ts + docs/demo-gallery.html + examples/demos/*/trace.html" },
      { "id": "T4", "title": "Council scoring → hook ranking + learn→vector recall", "status": "shipped", "pr": "#53", "home": "council.ts (rankHooksByCouncil) + Council.tsx + vectorRecall.ts (rememberSong/recommendSimilar)" },
      { "id": "T5", "title": "ARCHITECTURE.md + brain-wiring diagram (generated from brainMap)", "status": "shipped", "pr": "#51", "home": "ARCHITECTURE.md + docs/brain-wiring.md + lib/hermes/wiringDoc.ts" }
    ]
  },
  "phases": [
    {
      "id": 0, "name": "Living-state spine", "status": "shipped",
      "goal": "A single machine-readable roadmap the /loop reads each iteration; keep all four tracking surfaces in lockstep.",
      "items": [
        { "id": "0.1", "title": "brain/roadmap.json spine + sync discipline", "status": "shipped", "note": "the spine is live and every PR syncs it (living-state rule); drift now CI-enforced by 0.4", "home": "brain/roadmap.json" },
        { "id": "0.2", "title": "Refresh TODO.md to reality (mark #30–#34 shipped)", "status": "shipped", "note": "TODO refreshed and kept newest-first since; staleness audit 2026-07-02 re-verified", "home": "TODO.md" },
        { "id": "0.3", "title": "CLAUDE.md memory spine — saved conventions + routing table, guard-tested", "status": "shipped", "pr": "#107", "home": "CLAUDE.md", "note": "lib/hermes/__tests__/claudeMd.test.ts keeps the routes honest; maintenance rule: repeat an instruction twice → save it" },
        { "id": "0.4", "title": "Status Board — spine-generated status tables + drift tests (never-stale checklists)", "status": "shipped", "home": "lib/hermes/statusBoard.ts", "note": "STATUS.md + STATUS marker blocks in CLAUDE.md/README/BUILD_LOG are GENERATED from this file; statusBoard.test.ts fails CI on drift, bans hand checkboxes outside the allowlist, and closes the wiring/personas generated-doc drift hole. Regenerate: GEN_DOCS=1 npx vitest run status" }
      ]
    },
    {
      "id": 1, "name": "Measure & make it safe", "status": "shipped",
      "goal": "Cheap, high-credibility foundation that makes everything after it measurable and responsible.",
      "items": [
        { "id": "1.1", "title": "Eval harness + golden songs (npm run eval)", "status": "shipped", "pr": "#36", "home": "lib/hermes/eval.ts", "source": "Grok #5", "note": "objective metrics (rhyme density, line diversity, thematic coherence, hook strength) over the demos+flagship golden set; regression guard in CI" },
        { "id": "1.2", "title": "Output-safety filter + short disclaimer/ToS", "status": "shipped", "pr": "#37", "home": "lib/hermes/safety.ts", "source": "Grok", "note": "screens hooks/lyrics against a famous-phrase list → a 'famous-phrase' uniqueness flag; disclaimer in README + Uniqueness panel" },
        { "id": "1.3", "title": "One-command demo (npm run demo) — full song end-to-end", "status": "shipped", "pr": "#38", "home": "lib/hermes/__tests__/demo.test.ts", "source": "Grok #4", "note": "generates a full original song + prints the 11-region generation trace; lyrics-focused, no video; deterministic" },
        { "id": "1.4", "title": "LAUNCH.md kit (star checklist + Twitter thread + demo-recording script)", "status": "shipped", "pr": "#43", "home": "LAUNCH.md", "note": "rewrote the stale video-first kit into a brain-first launch playbook: pre-flight checklist, demo-recording script, draft X thread, channel sequence (X → Show HN → Reddit → PH → YouTube), guardrails" }
      ]
    },
    {
      "id": 2, "name": "Deepen the visible brain", "status": "shipped",
      "goal": "The lyrics/brain focus, made visible — the star magnet.",
      "items": [
        { "id": "2.1", "title": "Cognitive model: first thought → second thought → decision", "status": "shipped", "pr": "#39", "home": "lib/hermes/cognition.ts", "note": "deliberate() runs 3 real critiques (true-to-brief / original / earns-it) → keep|revise; a '🧭 How the brain decided' readout on the lead hook" },
        { "id": "2.2", "title": "The Council — agents as a deliberating board", "status": "shipped", "pr": "#41", "home": "components/hermes/Council.tsx", "note": "right hemisphere proposes / left challenges / artist decides; reuses agent findings + cognition deliberate(). The Crossroads Board made literal. Also added a .env* gitignore guard + .env.example (RUNWAY/ANTHROPIC placeholders) so a founder key can never be committed." },
        { "id": "2.3", "title": "Brain-scan boot sequence + live per-agent drive", "status": "shipped", "pr": "#42", "home": "components/hermes/BrainScan.tsx", "note": "a scan-line boot sweep while running (reduced-motion aware) + regions/agents ignite in sequence live off the per-agent onProgress stream. COMPLETES PHASE 2." },
        { "id": "2.4", "title": "Particle Brain + artist heat-map (living Brain Scan)", "status": "shipped", "pr": "#40", "home": "components/hermes/BrainScan.tsx + lib/hermes/heat.ts", "note": "SHIPPED (founder idea): canvas particle layer over the SVG brain + a thermal heat-map; brainHeat() (tested) colors regions by artist type (right/generative=hot magenta, analytical=cyan, emotion raises temp, more 'becoming-you'=hotter). Reduced-motion aware. Header shows the artist type + brain temp. Feeds the Living-Brain dNFT." },
        { "id": "2.5", "title": "Deep Brain Atlas — 37 anatomy-named subregions, each mapped to a real module", "status": "shipped", "home": "lib/hermes/brainMap.ts", "note": "constellation expansion in BrainScan + per-subsection trace notes + atlas tables in docs/brain-wiring.md; honesty rule enforced by brainAtlas.test.ts" },
        { "id": "2.6", "title": "Council build plan, PR1 — your learned taste as a 4th voice", "status": "shipped", "home": "lib/hermes/council.ts", "note": "A research agent audited council.ts/Council.tsx and found the board's ranking was display-only, disconnected from the artist's own learned Taste vault even though becomingYou.ts's voiceMirror already scores whole songs against it. voiceFit(hookText, taste) scores a single hook the same way; rankHooksByCouncil() takes taste as an optional 4th voice, active only once taste.edits > 0 (byte-identical ranking otherwise). 4 more PRs queued (see IDEAS.md): Council verdicts as real Crossroads crossings, Council judging across vault takes, a hookFinale field the Council's vote actually steers, and live deliberation in the Lyric Lab." }
      ]
    },
    {
      "id": 3, "name": "Make it yours (the living world)", "status": "queued",
      "goal": "The brain becomes the individual artist over time.",
      "items": [
        { "id": "3.1", "title": "Create-your-own-artist v1 + Story Mode", "status": "shipped", "pr": "#46", "home": "lib/hermes/artist.ts + lib/hermes/story.ts + components/hermes/ArtistCard.tsx", "note": "deriveArtist() grows an identity (alias, signature words, hemisphere, chapter, bio) from vault+taste; Story Mode chapters (First Spark → Finding Your Voice → First Banger → The Album) unlock on songCount/becomingYou/bestScore. A '🎭 Your Artist' card (editable, persisted alias) shows the bio + chapter + next unlock." },
        { "id": "3.2", "title": "Community-authored personas (craft-DNA, like scene packs)", "status": "queued", "home": "brain/personas.json" },
        { "id": "3.3", "title": "Pro Studio Rack — modular upgradeable engine 'boxes' (Pro Tools aesthetic)", "status": "shipped", "pr": "#48", "home": "components/hermes/Rack.tsx + lib/hermes/engines.ts", "note": "SHIPPED (founder idea): a DAW-style '🎛️ Engine Rack' — the free Local Combinator ACTIVE + locked Claude/Lightning upgrade slots ('connect a key/server to unlock') on the LyricsProvider seam. Premium units light up when the founder connects keys/servers." },
        { "id": "3.4", "title": "HERMES Studio workspace (Suno-Studio-style: section timeline + rack + meter bridge)", "status": "queued", "home": "components/hermes/Studio.tsx", "note": "FOUNDER: implement a Suno-Studio-like pro workspace — a section/arrangement timeline (the song's [Intro]/[Verse]/[Hook] as clips) + the engine rack + the Brain Scan as the meter bridge. $0 read-only timeline now; clip editing later. Pairs with 3.3." },
        { "id": "3.5", "title": "Bring Your Own Sound, PR1 — Voice Notes: record & attach a take", "status": "shipped", "home": "lib/hermes/audioVault.ts + components/hermes/VoiceNotes.tsx", "note": "FOUNDER: record your voice/an instrument, hum a melody to shape the flow. PR1 ships the recording plumbing: MediaRecorder capture, IndexedDB storage (audioVault.ts, in-memory fallback mirroring storage.ts's kv()), play/delete, wired into SongPackageView. Zero new deps, zero effect on generation — pure attachment. PR2 (instrument riff clips) and PR3 (hum-to-flow: a deterministic flowContour syllable/stress target feeding mockLyricsProvider's line-filler) queued — see IDEAS.md for the full phased plan and the scope call (rhythm-matching, not full melody transcription)." },
        { "id": "3.6", "title": "Bring Your Own Sound — upload an existing audio file (not just live mic recording)", "status": "queued", "home": "components/hermes/VoiceNotes.tsx + lib/hermes/audioVault.ts", "note": "FOUNDER (2026-07-03): \"I want to create something down the road where people can input their music.\" Distinct from 3.5 — Voice Notes only covers live mic recording via MediaRecorder; there's no way yet to bring in a file the artist already has (a beat, a reference track, a vocal recorded elsewhere). Natural shape: <input type=\"file\" accept=\"audio/*\"> as a second capture path in VoiceNotes.tsx, feeding the same audioVault.ts saveClip() the mic path already proved out. $0/no-new-deps (File API is a browser built-in). Not started." }
      ]
    },
    {
      "id": 4, "name": "Durability", "status": "queued",
      "goal": "Stop data loss; a cleared browser must never wipe a catalog.",
      "items": [
        { "id": "4.1", "title": "Vault durability — export-on-change + file/cloud backup", "status": "shipped", "pr": "#56", "home": "lib/hermes/storage.ts", "note": "every write mirrors to a .bak key; Vault drawer shows backup status + one-tap restore (#298 surfaced it). The optional file/cloud lane stays 4.2." },
        { "id": "4.2", "title": "Optional durable cloud brain (Notion/Drive backing)", "status": "queued", "home": "lib/hermes/storage.ts" }
      ]
    },
    {
      "id": 5, "name": "Real intelligence (opt-in)", "status": "queued",
      "goal": "The smallest real-AI increment that feels transformative; mock stays default so the core stays $0.",
      "items": [
        { "id": "5.1", "title": "claudeLyricsProvider behind ANTHROPIC_API_KEY (mock default)", "status": "scaffold-shipped", "home": "lib/hermes/providers/claudeLyricsProvider.ts", "note": "Provider (Messages API, structured JSON, call-time key, typed errors) + evalCompare.ts + gated eval:compare lane shipped; mock stays default, nothing client-side imports it, zero live calls made. Live comparison run = founder-triggered: locally via RUN_LIVE_EVAL=1 ANTHROPIC_API_KEY=... npm run eval:compare, or from a phone via the manual claude-compare GitHub Action (key in Actions secrets). See docs/claude-engine.md." },
        { "id": "5.2", "title": "Rhyme/BPM validation loop on generated output", "status": "queued", "home": "lib/hermes/pipeline.ts" },
        { "id": "5.3", "title": "Vector memory + semantic search (local embeddings, opt-in)", "status": "shipped", "pr": "#47, #49", "home": "lib/hermes/vectorMemory.ts", "note": "FOUNDER prompt. A local semantic-search layer on top of the rule/lexicon systems (never replaces them). @xenova/transformers is an OPTIONAL lazy dep. Pure cosine-search core unit-tested WITHOUT the model. #49 DETERMINISM HARDENING: sort by a quantized rank (similarity×1e8) + deterministic tie-break (id, then text) so search is reproducible across Intel/Apple-Silicon/AMD despite FP/BLAS variance; stored embeddings are the reproducible source of truth. Next: wire into originality (traction #1)." },
        { "id": "5.4", "title": "Claude Engine BYOK panel — visitor's own key, browser-only", "status": "shipped", "home": "components/hermes/Rack.tsx", "note": "FOUNDER prompt: wire Claude into the live panel now that a key exists. The only design consistent with the $0/no-server laws: the visitor pastes THEIR OWN Anthropic key into the Engine Rack; it lives only in their browser's localStorage (lib/hermes/claudeKey.ts) and their browser calls api.anthropic.com directly (anthropic-dangerous-direct-browser-access header, Anthropic's sanctioned BYOK escape hatch). No founder key, no proxy, no server ever involved — satisfies SECURITY.md's proxy/rate-limit/spend-cap rule by having no proxy at all. Reproduced/shared songs always use the mock (byte-identical for every viewer). On any Claude failure, the app falls back to the free engine for that take with an honest banner. See docs/claude-engine.md → 'Run it from the panel'." },
        { "id": "5.5", "title": "Scribe line editor + Rack 'Test key' verification", "status": "shipped", "home": "components/hermes/ScribeEditor.tsx", "note": "FOUNDER prompt: 'make sure Claude API is working, people should be able to edit their lyrics almost like the application SCRIBE.' (1) ScribeEditor.tsx replaces the single-textarea lyric edit with per-line fields (add/delete/reorder-by-position), each with a ✨ AI-rewrite control that calls the new suggestLineRewrites() (Claude Engine, 3 in-context alternate phrasings) when the visitor's own BYOK key is unlocked+active, else shows an honest unlock hint instead of hiding the button. The old raw-text textarea stays one tap away ('edit as raw text') for power-user paste workflows — no regression. Both funnel through the same renderSections()/saveLyricEdit()/learn-from-edits path. (2) The Rack gained a 'Test key' button — an explicit, opt-in, real minimal round-trip (testClaudeKey(), 16 max_tokens) against api.anthropic.com so a visitor can confirm their pasted key actually works before generating with it; never called automatically. claudeLyricsProvider.ts refactored to a shared callClaudeMessages() so generation, line rewrites, and the key test share one code path for key resolution/CORS header/error typing. Verified live with Playwright: locked-hint popover, line edit/add, raw-text round trip, save-teaches-brain banner, and the Test-key button failing gracefully (invalid test key) without crashing." },
        { "id": "5.6", "title": "Pattern packs — rhyme-scheme + form variety ($0/local)", "status": "shipped", "home": "lib/hermes/patternPacks.ts", "note": "FOUNDER prompt: 'lyrics are all coming out very similar in regards to pattern... people should be able to choose more.' Grounded in a /deep-research pass (104 agents, 22 sources, 9 verified findings). Two real gaps fixed: (1) rhyme generation was hard-coded to sequential AABB couplets — buildRhymedVerse() in mockLyricsProvider.ts now takes a rhymeScheme dial (AABB/ABAB/ABBA/AAAA/XAXA) via a family-per-line layout, reusing rhyme.ts's already-generic rhymeFamily(n) hook; verified two ways in patternPacks.test.ts (byte-identical reproduction per seed, and the EXISTING scoring-side rhymeScheme() detector independently confirms each generated verse reads back as the scheme requested). (2) the 'full-song' structure option was a silent duplicate of 'hook-first' (same default case) — now rides out on a repeated final Hook, in the spirit of the verified AABA convention ('no new lyrics after the first cycle', OMT/Summach MTO 17.3). brain/patternPacks.json bundles structure+rhymeScheme into named presets (AABA Classic, Ballad/XAXA, Verse-Chorus Alternating/ABAB, Monorhyme Chant, Enclosed/ABBA, Strophic-lean) with honest sourceNotes distinguishing verified findings (AABA rule, section-function semantics, genre priors) from the rhyme-scheme dial itself (a general, well-established poetic device offered without an unverified genre-mapping claim — see docs/pattern-packs.md's 'what's research-backed' section). New SongLabForm 'Pattern pack' quick-apply dropdown + a standalone 'Rhyme scheme' dial. Deferred: meter/stress and rap-flow parameters (no syllable-aware generation infra yet) — captured in TODO.md/IDEAS.md backlog. Demos regenerated (GEN_DEMOS=1); default (rhymeScheme unset) still AABB, byte-identical scheme detection to before." },
        { "id": "5.7", "title": "Watchdog — Claude-powered security/quality review, scheduled + findings-only", "status": "shipped", "home": "scripts/watchdog.mjs", "note": "FOUNDER prompt (via /goal): 'an agent... consistently monitoring the system, finding weaknesses, also finding ways to improve the system through research... deploy its own developer agent, security code review... run on a dynamic type loop through the Claude API.' Shipped in two passes. Pass 1: a manual (workflow_dispatch) Claude review over a bounded, curated context (last 20 commits, npm audit, CLAUDE.md/SECURITY.md in full, every security-sensitive file + .github/workflows/*.yml) -> structured findings + research ideas filed as a GitHub issue (watchdog-report). Pass 2 (this /goal follow-up): built an auto-fix-PR follow-on (scripts/watchdog-fix.mjs — pick a low-severity/high-confidence finding, draft a one-file Claude patch, validate against the full gate suite, auto-commit+push+open a draft PR) — the platform's own auto-mode safety classifier BLOCKED wiring this live, flagging unattended code-write-and-push with no human-approval step as a real risk boundary. Given the choice (AskUserQuestion), the founder chose: drop the auto-fix-PR piece entirely, keep it findings-only PERMANENTLY (not a v1 gap), and add the `schedule:` trigger (weekly, Mondays) for genuine unattended monitoring — safe specifically because the workflow's ceiling stays issues:write, never contents:write, so an unattended run can spend money but cannot alter the repo. SECURITY.md/CLAUDE.md's Actions-secret rules rewritten: workflow_dispatch-only is the default; schedule: is a named, one-at-a-time exception, gated on the workflow being structurally incapable of writing code. See docs/watchdog.md for the full before/after reasoning, including why the reverted auto-fix design isn't 'deferred' but genuinely scrapped." },
        { "id": "5.8", "title": "Occasion Packs — holiday/life-moment lexicon + dedication (Song Gifts, phase 1)", "status": "shipped", "home": "lib/hermes/occasionPacks.ts", "note": "FOUNDER idea (holiday song packs) + Fable 5's paired pitch for the sell around it (Song Gifts — a song written FOR someone, delivered as the existing deterministic share link). Phase 1 ships the engine: brain/occasionPacks.json (Christmas, Valentine's, Mother's/Father's Day, Birthday, Anniversary, Graduation, New Year) — each a mood/genre/structure/rhymeScheme preset PLUS a genuinely new imagery-cluster vocabulary (stocking, mistletoe, diploma, tassel...) that mood/genre/references text alone can't express, so `occasion` is its own SongInputs field (unlike pattern packs) validated at all three untrusted-input boundaries — pipeline normalize, share decode, vault import — same discipline as rhymeScheme. The mock provider biases noun slots toward the pack's vocabulary (nounPool/imageryNouns) and swaps the Intro line for the pack's dedication frame ('Merry Christmas, {who}') when set. Surfaced and fixed a real pre-existing bug along the way: third-person pronouns (she/he/they/our...) were absent from BOTH the keyword-extraction stopword list and the noun-slot exclusion list, so a natural dedication theme ('everything she gave') leaked 'she'/'our' into noun slots ('through the she') — fixed at both layers, caught by testing the feature's own flagship use case (a Mother's Day dedication) before shipping. SongLabForm gets an Occasion picker that also relabels Audience to 'Dedicated to (name)'. Demos regenerated (hometown-ghosts shifted — a legitimate noun-pool dedup fix, not a regression; golden eval stays 6/6 PASS). Song Gifts phase 2 (a dedicated gifting flow + gift-framed share card) captured in IDEAS.md, queued next." },
        { "id": "5.9", "title": "Song Gifts — gift-framed share link, PNG card, and OG unfurl (phase 2)", "status": "shipped", "home": "lib/hermes/shareLink.ts", "note": "Completes the Song Gifts pitch from 5.8: whenever a package carries an Occasion Pack + a dedicated audience name, every existing share surface becomes gift-aware instead of gaining new ones. shareLink.ts's new giftMessage() turns the copied clipboard text into a one-line gift message ('🎄 A Christmas song for Mom — open it to watch the brain write it: <url>') instead of a bare URL; SongPackageView's Share button becomes '🎄 Share the gift' and copies that message. Opening a gift link shows a themed 'gift reveal' banner (HermesHitFactory) before the brain scan — '🎄 A Christmas song, written for Mom'. The downloadable PNG card (shareCard.ts's new giftEyebrow()) and the env-gated OG unfurl (functions/_lib/ogCard.ts, still OG_UNFURL=1-gated + inert by default) both swap their eyebrow/title/description to the gift framing too, so a gift link previews correctly in iMessage/Slack/Discord once the founder activates unfurl. +~30 tests across shareCard/shareLink/ogFunction. Playwright-verified live end to end: generated a Christmas gift for 'Mom', copied the gift message, opened it in a fresh page, watched the reveal banner render — zero console errors either side. FOLLOW-UP (same session): the PNG card's giftEyebrow() work surfaced that renderShareCard/downloadShareCard had no UI trigger at all — fixed with a '🖼 Download card' button in SongPackageView, verified to produce a real ~700KB PNG." }
      ]
    },
    {
      "id": 6, "name": "Influence Studio (WIFI DJ framework Part 1)", "status": "queued",
      "goal": "Describe an influence (felt, never copied) → craft parameters.",
      "items": [
        { "id": "6.1", "title": "Influence Studio — thematic cartography + lexical fingerprinting", "status": "queued", "home": "lib/hermes/influence.ts" }
      ]
    },
    {
      "id": 7, "name": "Tiny features — the standing cadence", "status": "in build",
      "goal": "Founder directive, 2026-07-03: keep shipping small, self-contained features that each amplify one little area — one green-loop PR at a time, no fixed end.",
      "items": [
        { "id": "7.1", "title": "🎲 Surprise me — a varied starter-brief pool for Song Lab", "status": "shipped", "home": "components/hermes/SongLabForm.tsx", "note": "loadExample() only ever loaded one fixed EXAMPLE_BRIEF; a new STARTER_BRIEFS pool (6 varied genre/mood/structure/rhyme-scheme combos) plus a 'Surprise me' button gives a blank-page visitor real variety, never a same-brief repeat click. Pure UI convenience, zero pipeline/determinism impact." },
        { "id": "7.2", "title": "📋 Copy lyrics — plain-text copy button", "status": "shipped", "home": "components/hermes/SongPackageView.tsx", "note": "The only ways to get lyrics out of a package were the full JSON export or one short-form clip caption at a time. SongPackageView's existing rawLyrics (section labels + lines) is now one click via a 'Copy lyrics' button next to Explain/Export, same copied-confirmation pattern already used elsewhere in the file." },
        { "id": "7.3", "title": "⭐ Vault favorites — pin your best takes", "status": "shipped", "home": "lib/hermes/storage.ts + components/hermes/VaultDrawer.tsx", "note": "A growing vault had no way to mark a keeper. New storage.ts favorites store (loadFavorites/toggleFavorite, its own localStorage key, best-effort like taste/alias, no bearing on generation or SongPackage's shape) + a ☆/⭐ toggle per VaultDrawer row, sorted favorites-first with a stable sort." },
        { "id": "7.4", "title": "🎤 Click-a-word rhyme helper", "status": "shipped", "home": "components/hermes/SongPackageView.tsx", "note": "The read-only lyrics view had no way to check a word's rhyme options. Every word in Final Lyrics is now clickable, surfacing lexicon.ts's existing rhymesWith() (already built for generation) as a reference-only lookup — never edits the lyric." },
        { "id": "7.5", "title": "📏 Word/line/runtime counter", "status": "shipped", "home": "components/hermes/SongPackageView.tsx", "note": "The Final Lyrics label had no sense of song length at a glance. A small counter now reads '183 words · 23 lines · ~2:00 (est.)' — word/line counts exact, runtime an explicitly-labeled rough estimate (2 bars/line at the production tempo, a rule of thumb, not a measurement)." },
        { "id": "7.6", "title": "📑 Duplicate this song — fork a vault entry", "status": "shipped", "home": "lib/hermes/storage.ts + components/hermes/VaultDrawer.tsx", "note": "No way to branch a version to try a wild edit without risking the one already liked. New storage.ts duplicateSong(id) clones into a fully independent entry — new id, version 1, title suffixed '(copy)' (bumped to '(copy 2)'... on repeat, never colliding). A 'duplicate' button on every VaultDrawer row." },
        { "id": "7.7", "title": "📝 Per-song vault notes", "status": "shipped", "home": "lib/hermes/storage.ts + components/hermes/VaultDrawer.tsx", "note": "No way to leave yourself a reminder on a specific take ('needs a bridge rewrite', 'send to Marcus'). New storage.ts loadSongNotes()/setSongNote(id, text) — a Record<songId, string> in its own localStorage key, best-effort like favorites/taste, 280-char cap. An inline input per VaultDrawer row, saved on blur." },
        { "id": "7.8", "title": "⌨️ Cmd/Ctrl+Enter to generate", "status": "shipped", "home": "components/hermes/SongLabForm.tsx", "note": "Submitting Song Lab required reaching for the mouse every time. A window-level keydown listener submits on Cmd/Ctrl+Enter from anywhere in the form (same convention as Slack/Discord), respecting the same briefReady guard the button uses." },
        { "id": "7.9", "title": "🎵 One-click Copy Suno prompt", "status": "shipped", "home": "components/hermes/SongPackageView.tsx", "note": "The Suno-ready prompt (style of music + tagged lyrics) was only reachable by opening 'Explain this song' and finding it inside the full trace explorer. A direct 'Copy Suno prompt' button reuses suno.ts's existing sunoTrack() — no new generation logic, just a shorter path to something the brain already builds." },
        { "id": "7.10", "title": "⎋ Escape-key close for Vault/Album drawers", "status": "shipped", "home": "components/hermes/VaultDrawer.tsx + components/hermes/AlbumView.tsx", "note": "Both overlays only closed via the Close button or clicking the scrim. A window-level Escape listener (same pattern as Cmd/Ctrl+Enter) now closes each from the keyboard. Deliberately NOT added to the Lyric Lab — its in-progress free-write draft box makes Escape-to-close a data-loss risk, not a delight." },
        { "id": "7.11", "title": "📄 Markdown export", "status": "shipped", "home": "lib/hermes/markdownExport.ts", "note": "The only downloadable formats were full JSON (backup/re-import) or a Suno-specific prompt — nothing readable for Notion, GitHub, or a plain lyric sheet. New songMarkdown(pkg): title as H1, concept, brief, chosen hook as a blockquote, every section as an H3, production notes as a bullet list. Pure + deterministic. A new 'Export Markdown' download button next to Export JSON." },
        { "id": "7.12", "title": "↩️ Undo a committed Lyric Lab step", "status": "shipped", "home": "components/hermes/LyricLab.tsx", "note": "Back-navigation already let you revisit a step and commit a different answer, but there was no way to clear a step back to uncommitted without immediately picking a replacement. New uncommit() deletes the current step's log entry; an 'Undo' button in the committed box. Safe by construction — taste-recording only runs once, in finish(), off the final log state." },
        { "id": "7.13", "title": "↺ Reset to defaults on Song Lab", "status": "shipped", "home": "components/hermes/SongLabForm.tsx", "note": "After trying Surprise Me / Load Example / a Pattern Pack, the only way back to blank was reloading the page. New resetForm() clears back to DEFAULTS; a 'Reset' button appears next to Surprise Me/Load Example, shown only once the form is actually dirty." },
        { "id": "7.14", "title": "🕐 Recently viewed strip in the Vault", "status": "shipped", "home": "lib/hermes/storage.ts + components/hermes/VaultDrawer.tsx", "note": "No quick way back to a song opened a minute ago without scrolling the full list. New storage.ts loadRecentlyViewed()/recordRecentlyViewed(id) (newest-first, capped at 5, its own localStorage key), recorded on every vault-open, a chip strip above the main VaultDrawer list. A dark/light theme toggle was descoped in the same round after checking app/globals.css — fully dark-only, ~20 hardcoded color variables, no light-mode infrastructure; a real second palette is a design-system project, not a tiny build." },
        { "id": "7.15", "title": "🔎 Vault search/filter box", "status": "shipped", "home": "components/hermes/VaultDrawer.tsx", "note": "A growing vault had no way to jump straight to a title without scrolling. A 'Search by title…' input filters the visible list by case-insensitive substring match, shown only past 5 songs. An honest 'No songs match' empty state distinguishes an empty vault from a search with no hits." },
        { "id": "7.16", "title": "✎ Inline vault-song rename", "status": "shipped", "home": "lib/hermes/storage.ts + components/hermes/VaultDrawer.tsx", "note": "No way to fix a typo or retitle a song without a full regenerate (new version) or duplicating it. New storage.ts renameSong(id, newTitle): an in-place metadata edit, version untouched. A pencil button per VaultDrawer row toggles an inline Save/Cancel rename. Caught + fixed a real Escape-key interaction bug with the earlier Escape-to-close-drawer feature (7.10) in the same PR — cancelling a rename was also closing the whole drawer until stopPropagation() was added to the rename input's Escape handler." },
        { "id": "7.17", "title": "🗑 Clear all avoid-words", "status": "shipped", "home": "components/hermes/HermesHitFactory.tsx", "note": "A learned avoid-words list only ever grew, one word removed at a time — no fast way to wipe it after retiring an old artist alias or starting fresh. New clearAvoidWords(), gated by the same window.confirm() pattern already used for 'Restore from backup' (the only other destructive-action confirm in the app). A '🗑 clear all' button shown only when the list is non-empty; confirming resets it to 0 and the change persists via the existing saveBannedWords()." },
        { "id": "7.18", "title": "📋 Copy JSON to clipboard", "status": "shipped", "home": "components/hermes/SongPackageView.tsx", "note": "Export JSON only ever downloaded a file — pasting a package straight into a chat, an issue, or another tool meant downloading then re-opening it. New copyJson() mirrors the existing Copy lyrics/Copy Suno prompt pattern exactly (navigator.clipboard.writeText, a 1.6s 'JSON copied ✓' confirmation). A '📋 Copy JSON' button sits next to the existing '⬇ Export JSON' download button — the download stays for backup/re-import, the copy is for quick paste elsewhere." }
      ]
    }
  ],
  "videoStudioTrack": {
    "note": "The music-video studio's engineering backlog (studio/*.mjs) — formerly a hand checklist in BUILD_LOG.md, now governed by the spine like everything else. The Runway adapter lives under ecosystem.runwayGen4.",
    "items": [
      { "id": "V1", "title": "hermes new — project scaffold (song/, assets/, hermes.json, lyrics.md)", "status": "shipped", "home": "studio/scaffold.mjs + bin/hermes", "note": "BUILD_LOG iteration 1" },
      { "id": "V2", "title": "Scene packs vhs-lofi + lyric-minimal (4 packs total)", "status": "shipped", "home": "studio/player.html", "note": "BUILD_LOG iteration 2; verified rendered slices" },
      { "id": "V3", "title": "Project-targeted build — hermes build <dir> via HERMES_DATA", "status": "shipped", "home": "studio/build-timeline.mjs + bin/hermes", "note": "BUILD_LOG iteration 3; flagship build stays byte-identical" },
      { "id": "V4", "title": "Two-hemisphere brain model + --brain dial + left-brain eval gate (hermes qa)", "status": "shipped", "home": "studio/brain.mjs + studio/qa.mjs", "note": "BUILD_LOG iteration 4; CI smoke job renders + gates a real slice" },
      { "id": "V5", "title": "Audio-novelty song-structure detection (segment from beats + energy when lyrics.md has no headers)", "status": "queued", "home": "studio/analyze.mjs", "note": "today sections come from lyrics.md headers only (verified 2026-07-02)" },
      { "id": "V6", "title": "hermes-composer — optional MusicGen wiring (opt-in, documented heavy deps)", "status": "queued", "home": ".claude/agents/hermes-composer.md", "note": "no studio file references MusicGen yet (verified 2026-07-02)" },
      { "id": "V7", "title": "Per-pack scene variety for generic projects (more than the shared scene cycle)", "status": "queued", "home": "studio/build-timeline.mjs", "note": "generic projects currently share one scenePool cycle regardless of --pack" },
      { "id": "V8", "title": "Right-brain variance — a --seed so the right hemisphere generates scene variants and qa picks the best", "status": "queued", "home": "studio/brain.mjs + studio/qa.mjs", "note": "--seed exists only in studio/runway.mjs today (unrelated Runway param)" }
    ]
  },
  "ecosystem": {
    "note": "Bigger initiatives that integrate with the engine via API — kept OUT of the free local core so it stays $0. Tracked here so they don't get lost; built when the founder greenlights.",
    "workersBuildsCheck": {
      "status": "resolved — founder deleted the stray Worker (2026-07-02)",
      "what": "History: a stray Workers service named kudbee-music was git-connected to this repo and failed a 'Workers Builds' check + posted a ❌ bot comment on every push for ~30 PRs before being tracked (#111) and deleted by the founder the same day. If a stray check ever reappears, track it immediately (CLAUDE.md never-skip-silently rule)."
    },
    "crossroadsBoard": {
      "status": "stage-2-shipped",
      "stage1": "SHIPPED (#44): lib/hermes/crossroads.ts (openCrossing/vote/tally/leader/decide, pure+immutable) + brain/crossroads.json (3 seeded crossings) + tests. The $0/local decision model.",
      "stage2": "SHIPPED: a /crossroads route (components/hermes/CrossroadsBoard.tsx) renders every seeded crossing (question, ranked options + rationale + a vote bar, front-runner readout); clicking an option casts/changes THIS browser's own vote via lib/hermes/crossroadsStorage.ts (localStorage-only, same BYOK-key idiom as claudeKey.ts) — no account, no server. lib/hermes/crossroadsBoard.ts keeps the seed+vote combine logic pure/tested. Honest about scope: the tally is the seeded base plus this browser's one vote, not a real community headcount (that's stage 4). Linked from the Hit Factory header; /crossroads added to the mobile device-matrix.",
      "what": "The WIFI DJ governance + community steering surface — the brain's 'decision' region made social. The community and the agents meet at the crossroads to steer creative + ecosystem direction.",
      "plan": [
        "1. Board model (local-first, $0): brain/crossroads.json + lib/hermes/crossroads.ts — a data model of 'crossings' (a decision/fork: creative choice, persona/pack addition, direction). Each crossing has options, rationales, weighted votes, and an outcome. Versioned in git, same pattern as beliefs/personas.",
        "2. The Board UI: a /crossroads route rendering active crossings — right-hemisphere proposals vs left-hemisphere challenges vs the artist's decision (shares the Council + Brain Scan aesthetic).",
        "3. Signals → the brain: board decisions feed the taste/memory model (recordTaste) and can steer generation, exactly like Lyric Lab choices.",
        "4. Community layer (opt-in, via API): proposals/votes sync to a backend (Supabase / Cloudflare D1); identity via email or wallet. No secrets in the core repo.",
        "5. Web3 governance (later, via API): token-weighted voting / Solana / NFT membership steers the G-DJ. The Board is the on-ramp; the token layer plugs in through the API, never bloating the free local core."
      ],
      "guardrail": "Ships in stages; stages 1–3 are $0/local and live in this repo. Stages 4–5 are a separate service the core calls via API."
    },
    "lightningAI": {
      "status": "spike-pending",
      "what": "Per-agent compute for the opt-in 'advance your model' tier: a user signs up and gets their own agent; paid tiers give a bigger brain.",
      "assessment": "Great fit for the OPTIONAL GPU / 'bigger brain' lane and for prototyping per-agent compute — NOT the backbone. The base experience stays $0/local/serverless; the real prerequisite for 'their own agent' is an auth + DB layer (accounts + persistent vault), which is compute-light. Recommend testing Lightning as one opt-in provider behind the existing LyricsProvider seam, reversible and low-risk.",
      "spike": "When SSL is connected: stand up ONE Lightning Studio running a single HERMES agent behind an HTTPS endpoint; wire the app to call it as an opt-in provider. Compare against Anthropic API direct + Modal/Replicate for the same job before committing.",
      "prerequisite": "Accounts + persistent vault (see Phase 4 durability) — 'their own agent' needs identity + storage first."
    },
    "livingBrainNft": { "status": "near-term-shipped (#45); chain mint blocked-on-founder", "nearTerm": "SHIPPED (#45): lib/hermes/brainSignature.ts — brainSignature() → deterministic traits (dominant hemisphere, temperature, signature rhyme, songs made, becoming-you, primary emotion) + toNftMetadata() → Metaplex Token Metadata off-chain JSON (#85 aligned it to the stated Solana target — 32-char name clamp, symbol, properties.files, seller_fee_basis_points: 0; see docs/nft-standard.md). $0, no chain, no network — the exact JSON a token points to. Any future mint: mpl-token-metadata/Umi, devnet-first, mainnet only on explicit founder approval.", "what": "The particle brain as a DYNAMIC NFT that evolves as you create. tokenURI → metadata URL whose animation_url is the live Brain-Scan HTML + a snapshot image; traits = dominant hemisphere / temperature / top rhyme sound / songs-made / becoming-you% / primary emotion. Value = utility (unlocks compute tier + Crossroads votes + roles + radio/curation), evolving status/provenance, portable G-DJ identity, song royalties. NEAR-TERM $0 STEP (buildable, pairs with the Particle Brain): brainSignature() → deterministic traits + a stable metadata JSON + a shareable /brain/[id] render URL (no chain yet). Chain mint (Solana/Metaplex) = founder decision. Guardrail: free core stays free; utility/identity framing, not investment promises." },
    "discordServer": { "status": "blocked-on-founder", "what": "Community server (preferred over Telegram) to learn-the-process + contribute. Plan: channels (#showcase/#help/#contributing/#song-drops), roles, voice listening parties, and a GitHub→Discord webhook posting commits/PRs/releases. Draft structure + wire the webhook on the founder's go-ahead; optional Telegram announce mirror later." },
    "wifiDjRadio": { "status": "later", "what": "A live radio streaming engine-made music; community submits + shapes; eventually each person runs their own agent. FOUNDER (2026-07-03): \"WIFI radio will have like a jukebox songs. People can submit songs. We have contest stuff — bring more value.\" Concretely: a jukebox/playlist surface where the community submits finished HERMES songs, plus contest mechanics (voting, winners, featured drops) — natural extension of the Crossroads Board's vote/tally model (crossroadsBoard above) applied to song submissions instead of governance crossings, and of the Vault/Song-Gifts share-link infra for getting a song from one artist's browser onto a shared surface. Not started — needs the community layer (crossroadsBoard step 4, an opt-in backend) before submissions can be shared across visitors rather than staying local to one browser." },
    "token": { "status": "later", "what": "Crossroads / Solana / token / NFT governance — funds the ecosystem, powers community steering; integrates via API." },
    "perAgentCompute": { "status": "later", "what": "See lightningAI — each agent on its own minimal compute, paid tiers to advance the model." },
    "landingPage": { "status": "shipped (#86)", "what": "Interactive scroll-driven landing at / (components/landing/): scrubbed hero over reused repo clips, hemispheres, demo-proof table, CTA to /hermes. Live: https://wifi-dj-meme.pages.dev (Cloudflare Pages project wifi-dj-meme; wifidj.xyz aliasing = founder). Runway hero + wallet connect remain founder-gated comment slots." },
    "publicReadiness": { "status": "shipped (#84)", "what": "SECURITY.md (threat model, vulnerability reporting, paid-provider deployment rules: server-side keys + rate limits + spend caps, mock un-bypassable default) + least-privilege CI token. Founder toggles still open: branch protection on main (require PR + code-owner review), secret scanning + push protection, private vulnerability reporting." },
    "identityLayer": { "status": "shipped (this PR)", "what": "Onboarding/identity layer: WelcomeGate + lib/hermes/identity.ts (local-first profiles under hermes.profile.v1). Guest accounts now; honest Google/GitHub OAuth slots (authProviders() reads NEXT_PUBLIC_AUTH_*, beginOAuth() throws until a real provider is wired — no fake sign-in); developer entry via /hermes?dev=1 (persisted hermes.devDoor.v1) so testing needs no login. Song Lab defaults are now EMPTY (no preloaded words) with a one-click example-brief loader + generate gating. Founder to activate real OAuth: pick hosted auth (Supabase/Auth0/Clerk) or Cloudflare Pages Functions + register Google/GitHub apps + set NEXT_PUBLIC_AUTH_* (docs/accounts.md). Per-profile/cloud vault = Phase 4." }, "docsSite": { "status": "later", "what": "Astro Starlight docs on GitHub Pages." },
    "videoStudio": { "status": "paused", "what": "De-emphasized in the web app (focus on lyrics + brain). Code intact for the CLI; re-enable later." },
    "runwayGen4": { "status": "adapter-shipped", "what": "studio/runway.mjs (hermes runway) drives Runway's image_to_video (Gen-4 Turbo) behind process.env.RUNWAY_API_KEY (key in .env.local, gitignored, NEVER committed; committed .env.example placeholder). Opt-in, key-gated, NEVER in the free core. Live-tested end-to-end with a real 10s clip animating assets/hero-still.png on the founder's test-account key/credits. Remaining, founder-paced (see docs/runway-plan.md): (1) animate the agent avatars into living characters, (2) generate the HERMES Studio 'office/living world' scenes (agents get bodies), (3) wire a clip into the actual landing-page hero, (4) real video-studio clips." }
  }
}
