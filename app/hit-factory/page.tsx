import HermesHitFactory from '@/components/hermes/HermesHitFactory';

// Alias route — same command deck at /hit-factory and /hermes.
export default function HitFactoryPage() {
  return <HermesHitFactory />;
}
